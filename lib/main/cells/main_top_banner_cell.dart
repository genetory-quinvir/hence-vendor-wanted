import 'package:flutter/material.dart';

class MainTopBannerCell extends StatelessWidget {
  const MainTopBannerCell({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 240,
      color: Colors.red,
    );
  }
}
